---
phase: 01-daily-task-list-data-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - HomeworkPlan/Repositories/TaskRepository.swift
  - HomeworkPlan/Views/Tabs/TodayView.swift
  - HomeworkPlan/Views/Tabs/AllTasksView.swift
  - HomeworkPlan/ViewModels/AllTasksViewModel.swift
  - HomeworkPlanTests/HomeworkPlanTests/TaskRepositoryTests.swift
autonomous: true
requirements:
  - TASK-06
must_haves:
  truths:
    - "今日 Tab 顶部日期选择器可切换查看任意日期"
    - "切换日期后列表显示该日未完成作业"
    - "全部 Tab 按日期分组展示历史与未来任务"
    - "全部 Tab 可看到已完成与未完成任务"
  artifacts:
    - path: HomeworkPlan/Views/Tabs/TodayView.swift
      provides: "DatePicker for selected day"
      contains: "DatePicker"
    - path: HomeworkPlan/Views/Tabs/AllTasksView.swift
      provides: "Date-grouped all tasks list"
      min_lines: 50
  key_links:
    - from: HomeworkPlan/Views/Tabs/TodayView.swift
      to: HomeworkPlan/Repositories/TaskRepository.swift
      via: "fetch by calendar day"
      pattern: "fetchTasks\\(dueOn:"
    - from: HomeworkPlan/Views/Tabs/AllTasksView.swift
      to: HomeworkPlan/Repositories/TaskRepository.swift
      via: "fetchAll grouped"
      pattern: "fetchAllTasksGroupedByDate"
---

<objective>
实现日期浏览：今日 Tab 日期选择器 + 全部 Tab 按日分组列表（TASK-06）。

Purpose: 家长可查看明天/历史作业，不仅限于今天。
Output: TaskRepository 日期查询 + TodayView 日期状态 + AllTasksView。
</objective>

<execution_context>
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md
@docs/PRD.md (3.6 支持切换日期查看历史)
@.planning/research/ARCHITECTURE.md (AllTasksView)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskRepository date-range fetch tests</name>
  <files>HomeworkPlan/Repositories/TaskRepository.swift, HomeworkPlanTests/HomeworkPlanTests/TaskRepositoryTests.swift</files>
  <read_first>
    - HomeworkPlan/Repositories/TaskRepository.swift
    - HomeworkPlan/Models/HomeworkTask.swift
  </read_first>
  <behavior>
    - testFetchTasksDueOn_returnsOnlyMatchingCalendarDay: tasks on today and tomorrow → fetchDueOn(today) returns only today's
    - testFetchAllGroupedByDate_groupsCorrectly: three tasks on two dates → two section keys
  </behavior>
  <action>
    Add TaskRepository.fetchTasks(dueOn: Date, includeCompleted: Bool) using Calendar.current startOfDay/endOfDay predicate on dueDate. Add fetchAllTasksGroupedByDate() returning [Date: [HomeworkTask]] sorted descending by date. Write tests in TaskRepositoryTests with in-memory container first, implement to GREEN. Use Calendar.current for timezone-safe day boundaries.
  </action>
  <acceptance_criteria>
    - TaskRepository.swift contains fetchTasks(dueOn:includeCompleted:) and fetchAllTasksGroupedByDate()
    - Both new tests pass
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:HomeworkPlanTests/TaskRepositoryTests test 2>&1 | tail -3 | grep -q 'TEST SUCCEEDED'</automated>
  </verify>
  <done>Date-scoped fetch logic tested and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Today tab date picker</name>
  <files>HomeworkPlan/Views/Tabs/TodayView.swift</files>
  <read_first>
    - HomeworkPlan/Views/Tabs/TodayView.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (D-01: 顶部日期选择器)
  </read_first>
  <action>
    Per CONTEXT 今日主界面布局 (D-01): Add @State selectedDate: Date default Date() at top of TodayView. Show compact DatePicker 选择日期 bound to selectedDate with .datePickerStyle(.compact) and Chinese locale. Replace static @Query filter with fetch/filter for selectedDate calendar day incomplete tasks. When selectedDate is not today, show subtle 中文 banner「正在查看 {formatted date}」. Keep add-task default dueDate = selectedDate. accessibilityIdentifier today-date-picker on DatePicker.
  </action>
  <acceptance_criteria>
    - TodayView.swift contains DatePicker and @State selectedDate
    - Task list updates when selectedDate changes (predicate uses startOfDay)
    - New task dueDate defaults to selectedDate
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>User can browse incomplete tasks for any selected date on Today tab.</done>
</task>

<task type="auto">
  <name>Task 3: All Tasks tab grouped by date</name>
  <files>HomeworkPlan/Views/Tabs/AllTasksView.swift, HomeworkPlan/ViewModels/AllTasksViewModel.swift</files>
  <read_first>
    - HomeworkPlan/Views/Tabs/AllTasksView.swift
    - HomeworkPlan/Views/Tasks/TaskRowView.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (D-04: 全部 Tab 按日期分组)
    - docs/PRD.md section 全部Tab
  </read_first>
  <action>
    Per CONTEXT 导航结构 (D-04) TASK-06: Replace AllTasksView placeholder with List grouped by due date sections (今天/昨天/具体日期 中文 formatted). Include both complete and incomplete tasks; completed rows show strikethrough or checkmark style. Reuse TaskRowView with same complete/edit/delete interactions. Optional AllTasksViewModel @Observable if logic exceeds ~40 lines; otherwise inline in view. Empty state: 中文「暂无作业记录」. Sort sections: today first, then future ascending, past descending — or simple descending by date for MVP clarity.
  </action>
  <acceptance_criteria>
    - AllTasksView.swift renders multiple date Sections with TaskRowView rows
    - Completed tasks visible with distinct completed styling
    - Tab label 全部 navigates to this view from MainTabView
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>All Tasks tab shows full task history grouped by due date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DatePicker input → query | User-selected date drives fetch scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Date boundary calculation | mitigate | Always use Calendar.current startOfDay for predicates; unit test timezone edges |
</threat_model>

<verification>
- TaskRepository date tests pass
- Switching Today date shows correct tasks
- All tab lists tasks across multiple dates
</verification>

<success_criteria>
- TASK-06 fully satisfied
- ROADMAP success criterion #3 true: user can browse non-today dates
</success_criteria>

## Artifacts this phase produces

- `TaskRepository.fetchTasks(dueOn:includeCompleted:)`
- `TaskRepository.fetchAllTasksGroupedByDate()`
- `AllTasksViewModel` — optional grouping state helper
- Updated `TodayView` — date picker + selected-day filtering
- Updated `AllTasksView` — date-section grouped task list

<output>
Create `.planning/phases/01-daily-task-list-data-foundation/01-03-SUMMARY.md` when done
</output>
