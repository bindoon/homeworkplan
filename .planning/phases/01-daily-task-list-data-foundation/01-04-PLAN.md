---
phase: 01-daily-task-list-data-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - HomeworkPlan/Repositories/SubjectRepository.swift
  - HomeworkPlan/Views/Tabs/SettingsView.swift
  - HomeworkPlan/Views/Settings/SubjectManagementView.swift
  - HomeworkPlan/Views/Settings/SubjectFormView.swift
  - HomeworkPlan/Views/Tasks/ManualTaskFormView.swift
  - HomeworkPlan/Views/Tasks/TaskEditView.swift
  - HomeworkPlanTests/HomeworkPlanTests/SubjectRepositoryTests.swift
autonomous: true
requirements:
  - TASK-07
must_haves:
  truths:
    - "设置 Tab 可查看所有科目含默认与自定义"
    - "用户可添加自定义科目（名称 + emoji）"
    - "用户可编辑或删除非默认自定义科目"
    - "默认四科目不可删除"
    - "手动添加/编辑任务时可选择自定义科目"
  artifacts:
    - path: HomeworkPlan/Views/Settings/SubjectManagementView.swift
      provides: "Subject CRUD UI"
      min_lines: 40
    - path: HomeworkPlan/Repositories/SubjectRepository.swift
      provides: "create/update/delete subject with normalizedName dedupe"
      contains: "normalizedName"
  key_links:
    - from: HomeworkPlan/Views/Settings/SubjectManagementView.swift
      to: HomeworkPlan/Repositories/SubjectRepository.swift
      via: "CRUD actions"
      pattern: "subjectRepository\\."
    - from: HomeworkPlan/Views/Tasks/ManualTaskFormView.swift
      to: HomeworkPlan/Models/Subject.swift
      via: "subject picker lists all subjects"
      pattern: "@Query.*Subject"
---

<objective>
实现科目管理：设置页 CRUD + 任务表单科目选择器覆盖自定义科目（TASK-07 完整）。

Purpose: 支持补习班等自定义科目，默认四科不可删。
Output: SubjectManagementView + SubjectRepository 扩展 + 表单 picker 更新。
</objective>

<execution_context>
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md
@.planning/research/PITFALLS.md (Subject dedupe via normalizedName)
@docs/PRD.md (科目 Subject section)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SubjectRepository CRUD and dedupe tests</name>
  <files>HomeworkPlan/Repositories/SubjectRepository.swift, HomeworkPlanTests/HomeworkPlanTests/SubjectRepositoryTests.swift</files>
  <read_first>
    - HomeworkPlan/Repositories/SubjectRepository.swift
    - HomeworkPlan/Models/Subject.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (科目管理 D-03)
    - .planning/research/PITFALLS.md (normalizedName dedupe)
  </read_first>
  <behavior>
    - testCreateCustomSubject_persistsWithNormalizedName: create「奥数」→ normalizedName「奥数」lowercased
    - testCreateDuplicateName_throwsOrReturnsExisting: two「钢琴」→ second rejected or returns existing per repository design
    - testDeleteDefaultSubject_forbidden: delete subject where isDefault==true → throws or returns false
    - testDeleteCustomSubject_succeeds: custom subject deleted
  </behavior>
  <action>
    Extend SubjectRepository: create(name:emoji:), update(id:name:emoji:), delete(id:), fetchAll() sorted by sortOrder. On create/update set normalizedName = name.trimmingCharacters(in: .whitespaces).lowercased(). Reject duplicate normalizedName with SubjectError.duplicateName. delete() guard isDefault==false else throw SubjectError.cannotDeleteDefault. Add SubjectRepositoryTests with in-memory container. Implement to GREEN.
  </action>
  <acceptance_criteria>
    - SubjectRepository.swift contains create, update, delete with normalizedName logic
    - SubjectRepositoryTests all four tests pass
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:HomeworkPlanTests/SubjectRepositoryTests test 2>&1 | tail -3 | grep -q 'TEST SUCCEEDED'</automated>
  </verify>
  <done>Subject CRUD with dedupe and default-protection tested.</done>
</task>

<task type="auto">
  <name>Task 2: Settings subject management UI</name>
  <files>HomeworkPlan/Views/Tabs/SettingsView.swift, HomeworkPlan/Views/Settings/SubjectManagementView.swift, HomeworkPlan/Views/Settings/SubjectFormView.swift</files>
  <read_first>
    - HomeworkPlan/Views/Tabs/SettingsView.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (D-04: 设置 Tab 科目管理)
  </read_first>
  <action>
    Per CONTEXT 导航结构 (D-04) and 科目管理 (D-03): Replace SettingsView placeholder with NavigationStack. Sections: 科目管理 NavigationLink → SubjectManagementView listing all subjects (emoji + name). Default subjects show 默认 badge; swipe delete only on isDefault==false. Toolbar + button opens SubjectFormView sheet for new custom subject (name TextField, emoji TextField default 📚). Tap row edits custom subjects; default subjects read-only or name-locked. 关于 section with app name HomeworkPlan version 1.0.0 中文. NO API Key section yet (Phase 2 placeholder text「AI 解析设置将在后续版本提供」). Chinese UI labels throughout.
  </action>
  <acceptance_criteria>
    - SettingsView.swift contains NavigationLink to SubjectManagementView
    - SubjectManagementView lists subjects with add and swipe-delete for custom only
    - SubjectFormView creates/edits custom subjects via SubjectRepository
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>Settings tab provides full subject management UX.</done>
</task>

<task type="auto">
  <name>Task 3: Wire custom subjects into task forms</name>
  <files>HomeworkPlan/Views/Tasks/ManualTaskFormView.swift, HomeworkPlan/Views/Tasks/TaskEditView.swift</files>
  <read_first>
    - HomeworkPlan/Views/Tasks/ManualTaskFormView.swift
    - HomeworkPlan/Views/Tasks/TaskEditView.swift
  </read_first>
  <action>
    Ensure ManualTaskFormView and TaskEditView subject Picker uses @Query(Subject.self, sort: \\Subject.sortOrder) so newly created custom subjects appear immediately without app restart. Picker displays emoji + name. Verify creating task with custom subject「钢琴」shows under 钢琴 section on Today view.
  </action>
  <acceptance_criteria>
    - Both form views use @Query for Subject list
    - Custom subject created in Settings appears in task form picker
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>Custom subjects selectable when creating/editing tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subject name input → SwiftData | User-defined subject names persisted and synced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | SubjectFormView | mitigate | Trim name; reject empty; normalizedName dedupe prevents duplicate entries |
| T-01-08 | Denial of Service | Delete default subject | mitigate | Repository guard isDefault; UI hides delete for defaults |
</threat_model>

<verification>
- SubjectRepositoryTests pass
- Add custom subject in Settings → visible in task picker → task grouped correctly
- Default subjects cannot be deleted
</verification>

<success_criteria>
- TASK-07 fully satisfied
- ROADMAP success criterion #4 true
</success_criteria>

## Artifacts this phase produces

- `SubjectRepository.create/update/delete` with normalizedName dedupe
- `SubjectError` — duplicateName, cannotDeleteDefault
- `SubjectManagementView` — settings subject list CRUD
- `SubjectFormView` — add/edit custom subject sheet
- `SubjectRepositoryTests` — unit tests
- Updated `SettingsView` — navigation to subject management + 关于

<output>
Create `.planning/phases/01-daily-task-list-data-foundation/01-04-SUMMARY.md` when done
</output>
