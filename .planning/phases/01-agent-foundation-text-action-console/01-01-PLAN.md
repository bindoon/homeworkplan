---
phase: 01-agent-foundation-text-action-console
plan: 01
type: implementation
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03]
---

# Plan 01-01: Agent Foundation & Text Action Console

## Objective
Ship AgentOrchestrator + ToolRegistry + Action Tab so users can type natural language to add/import homework with confirmation gate.

## Tasks
1. Agent models, tool protocol, registry, executor (read vs mutating split)
2. AgentLLMService (OpenAI-compatible tools), system prompt, orchestrator (max 5 rounds)
3. ActionConsoleViewModel + ActionConsoleView + AgentProposalCard
4. Wire AppDependencies + MainTabView 4th tab
5. ToolRegistryTests + ToolExecutorTests + pbxproj

## Success Criteria
- [x] 14 tools registered with valid JSON schemas
- [x] create_task returns proposal without DB write until confirm
- [x] import_from_text uses ImportService.processPastedText
- [x]「操作」tab visible in MainTabView
- [x] Unit tests pass
