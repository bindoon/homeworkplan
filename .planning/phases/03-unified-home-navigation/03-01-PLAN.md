---
phase: 03-unified-home-navigation
plan: 01
type: execute
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, NAV-01]
---

# Phase 3 Plan 01: Unified Home & Navigation

## Objective

Merge TodayView + AllTasksView into a single Home tab with date picker, subject grouping, and collapsible history sections. MainTabView becomes Home + Action + Settings.

## Tasks

1. Create HomeQueryViewModel — unified selected-date + history section state
2. Create HomeQueryView — merged UX with DisclosureGroup sections
3. Update MainTabView — replace Today + All with Home
4. Deprecate TodayView / AllTasksView (keep files, remove from tab shell)
5. Migrate clipboard banner and minimal add/import toolbar from TodayView
6. HomeQueryViewModelTests — collapse logic and date filtering
7. Phase docs and traceability updates

## Success Criteria

- App shows Home + Action + Settings only
- Home defaults to today, grouped by subject, collapsible
- History sections collapsed except today
- Toggle completion from home list
- Unit tests pass
