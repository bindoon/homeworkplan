---
phase: 01-daily-task-list-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - HomeworkPlan/Repositories/TaskRepository.swift
  - HomeworkPlan/Views/Tasks/TaskRowView.swift
  - HomeworkPlan/Views/Tasks/TaskEditView.swift
  - HomeworkPlan/Views/Tabs/TodayView.swift
  - HomeworkPlanTests/HomeworkPlanTests/TaskRepositoryTests.swift
autonomous: true
requirements:
  - TASK-03
  - TASK-04
  - TASK-05
must_haves:
  truths:
    - "用户可勾选 checkbox 标记作业完成并记录 completedAt"
    - "用户可取消完成状态"
    - "用户点击任务行打开编辑 sheet 修改科目/内容/截止日期/备注"
    - "用户左滑可删除任务"
    - "已完成任务从今日未完成列表消失"
  artifacts:
    - path: HomeworkPlan/Repositories/TaskRepository.swift
      provides: "markComplete, update, delete methods"
      contains: "markComplete"
    - path: HomeworkPlan/Views/Tasks/TaskEditView.swift
      provides: "Edit sheet for existing task"
      min_lines: 30
  key_links:
    - from: HomeworkPlan/Views/Tasks/TaskRowView.swift
      to: HomeworkPlan/Repositories/TaskRepository.swift
      via: "toggle complete / delete"
      pattern: "markComplete|delete"
    - from: HomeworkPlan/Views/Tabs/TodayView.swift
      to: HomeworkPlan/Views/Tasks/TaskEditView.swift
      via: "sheet on row tap"
      pattern: "TaskEditView"
---

<objective>
完成今日列表上的任务生命周期：标记完成/取消、编辑、左滑删除（TASK-03/04/05）。

Purpose: 家长可维护清单准确性，无需重建任务。
Output: TaskRepository 完整写操作 + TaskEditView + TaskRow 交互。
</objective>

<execution_context>
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaskRepository completion and delete unit tests</name>
  <files>HomeworkPlanTests/HomeworkPlanTests/TaskRepositoryTests.swift, HomeworkPlan/Repositories/TaskRepository.swift</files>
  <read_first>
    - HomeworkPlan/Repositories/TaskRepository.swift
    - HomeworkPlan/Models/HomeworkTask.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (数据模型 D-02: completedAt field)
  </read_first>
  <behavior>
    - testMarkComplete_setsIsCompletedAndCompletedAt: create task → markComplete → isCompleted true AND completedAt not nil
    - testMarkIncomplete_clearsCompletedAt: completed task → markIncomplete → isCompleted false AND completedAt nil
    - testDelete_removesTask: create → delete → fetch returns nil/count 0
    - testUpdate_changesFields: update subject/content/notes/dueDate → persisted values match
  </behavior>
  <action>
    Add HomeworkPlanTests unit test target if missing. Use in-memory ModelContainer (cloudKitDatabase .none) for tests. Extend TaskRepository with markComplete(id:), markIncomplete(id:), update(id:subject:content:notes:dueDate:), delete(id:). markComplete sets isCompleted=true and completedAt=Date(); markIncomplete clears completedAt. Write tests first (RED), then implement methods (GREEN).
  </action>
  <acceptance_criteria>
    - TaskRepositoryTests.swift contains all four test functions
    - TaskRepository.swift exports markComplete, markIncomplete, update, delete
    - xcodebuild test for TaskRepositoryTests exits TEST SUCCEEDED
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:HomeworkPlanTests/TaskRepositoryTests test 2>&1 | tail -3 | grep -q 'TEST SUCCEEDED'</automated>
  </verify>
  <done>Repository CRUD completion/update/delete covered by passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: TaskRow complete toggle and swipe delete</name>
  <files>HomeworkPlan/Views/Tasks/TaskRowView.swift, HomeworkPlan/Views/Tabs/TodayView.swift</files>
  <read_first>
    - HomeworkPlan/Views/Tasks/TaskRowView.swift
    - HomeworkPlan/Views/Tabs/TodayView.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (今日主界面 D-01: checkbox, 左滑删除)
  </read_first>
  <action>
    Per CONTEXT 今日主界面布局 (D-01) TASK-03/05: Enable TaskRowView checkbox binding — tap toggles complete via TaskRepository.markComplete/markIncomplete. Add light haptic on complete (UIImpactFeedbackGenerator). TodayView .swipeActions trailing destructive 删除 calling TaskRepository.delete. Filter @Query or predicate to show only isCompleted==false tasks for selected day. Completed tasks disappear from list immediately after toggle.
  </action>
  <acceptance_criteria>
    - TaskRowView contains Toggle or Button for completion with accessibilityIdentifier task-complete-toggle
    - TodayView list uses swipeActions with 删除 label
    - @Query or filter excludes isCompleted tasks from today incomplete list
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>User can complete/uncomplete and swipe-delete from Today list.</done>
</task>

<task type="auto">
  <name>Task 3: Task edit sheet on row tap</name>
  <files>HomeworkPlan/Views/Tasks/TaskEditView.swift, HomeworkPlan/Views/Tabs/TodayView.swift</files>
  <read_first>
    - HomeworkPlan/Views/Tasks/ManualTaskFormView.swift (reuse form patterns)
    - HomeworkPlan/Repositories/TaskRepository.swift
    - .planning/phases/01-daily-task-list-data-foundation/01-CONTEXT.md (D-01: 点击进入编辑 sheet, TASK-04)
  </read_first>
  <action>
    Per CONTEXT D-01 TASK-04: Tap task row (not checkbox) presents TaskEditView sheet pre-filled with task fields — subject picker, content, dueDate, notes. Save calls TaskRepository.update; Cancel dismisses without save. Reuse styling from ManualTaskFormView for consistency. accessibilityIdentifier task-edit-save on save button.
  </action>
  <acceptance_criteria>
    - TaskEditView.swift exists with init taking HomeworkTask
    - TodayView presents sheet on row tap via .sheet(item:) or selectedTask state
    - Save invokes TaskRepository.update with edited values
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>User can edit all task fields from Today list via edit sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI gesture → Repository | Complete/delete/edit actions mutate persisted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | TaskEditView | mitigate | Validate non-empty content on save; same rules as create |
| T-01-05 | Repudiation | completedAt timestamp | accept | Local device clock; no audit requirement for MVP |
</threat_model>

<verification>
- TaskRepositoryTests pass
- Manual verify: complete task → disappears from today; edit content → list updates; swipe delete → gone
</verification>

<success_criteria>
- TASK-03, TASK-04, TASK-05 satisfied on Today tab
- completedAt recorded on complete, cleared on uncomplete
</success_criteria>

## Artifacts this phase produces

- `TaskRepository.markComplete(_:)` / `markIncomplete(_:)` / `update(_:)` / `delete(_:)`
- `TaskEditView` — edit sheet for existing HomeworkTask
- `TaskRepositoryTests` — unit tests for write operations
- Updated `TaskRowView` — completion toggle + row tap affordance
- Updated `TodayView` — swipe delete + edit sheet presentation

<output>
Create `.planning/phases/01-daily-task-list-data-foundation/01-02-SUMMARY.md` when done
</output>
