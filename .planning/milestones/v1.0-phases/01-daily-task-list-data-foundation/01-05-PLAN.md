---
phase: 01-daily-task-list-data-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-04
files_modified:
  - HomeworkPlan/App/HomeworkPlanApp.swift
  - HomeworkPlan/Services/Sync/CloudKitSchemaInitializer.swift
  - HomeworkPlan/Services/Sync/SubjectDedupeService.swift
  - HomeworkPlan/Repositories/SubjectRepository.swift
  - HomeworkPlan/Resources/PrivacyInfo.xcprivacy
  - HomeworkPlan/README.md
  - HomeworkPlanTests/HomeworkPlanTests/SubjectDedupeServiceTests.swift
autonomous: false
requirements:
  - SETT-03
user_setup:
  - service: iCloud
    why: "Multi-device sync verification requires two devices or Simulator + physical device signed into same Apple ID"
    dashboard_config:
      - task: "Sign into iCloud on test devices with same Apple ID"
        location: "Settings → Apple ID → iCloud → HomeworkPlan toggle ON"

must_haves:
  truths:
    - "SwiftData ModelConfiguration 使用 CloudKit private database"
    - "DEBUG 构建首次启动初始化 CloudKit schema"
    - "Subject normalizedName 去重逻辑在远程变更后可合并重复科目"
    - "PrivacyInfo.xcprivacy 存在满足 App Store 要求"
    - "README 记录 xcodebuild 命令与 iCloud 同步验证步骤"
  artifacts:
    - path: HomeworkPlan/Services/Sync/CloudKitSchemaInitializer.swift
      provides: "DEBUG schema init"
      contains: "initializeCloudKitSchema"
    - path: HomeworkPlan/README.md
      provides: "Build and iCloud sync verification docs"
      contains: "xcodebuild"
  key_links:
    - from: HomeworkPlan/App/HomeworkPlanApp.swift
      to: HomeworkPlan/Services/Sync/CloudKitSchemaInitializer.swift
      via: "call on first launch DEBUG"
      pattern: "CloudKitSchemaInitializer"
    - from: HomeworkPlan/App/HomeworkPlanApp.swift
      to: CloudKit
      via: "ModelConfiguration cloudKitDatabase"
      pattern: "cloudKitDatabase"
---

<objective>
完成 SETT-03 iCloud 同步硬化：CloudKit schema 初始化、科目去重、隐私清单与双设备验证文档。

Purpose: 多设备数据一致性；避免 PITFALLS #5 CloudKit 重复科目。
Output: Sync 服务 + README + checkpoint 人工验证 iCloud。
</objective>

<execution_context>
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md (initializeCloudKitSchema DEBUG)
@.planning/research/PITFALLS.md (Pitfall 5 SwiftData+CloudKit duplicate)
@.planning/phases/01-daily-task-list-data-foundation/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CloudKit schema initializer and subject dedupe service</name>
  <files>HomeworkPlan/Services/Sync/CloudKitSchemaInitializer.swift, HomeworkPlan/Services/Sync/SubjectDedupeService.swift, HomeworkPlan/App/HomeworkPlanApp.swift, HomeworkPlanTests/HomeworkPlanTests/SubjectDedupeServiceTests.swift</files>
  <read_first>
    - HomeworkPlan/App/HomeworkPlanApp.swift
    - .planning/research/STACK.md (NSPersistentCloudKitContainer.initializeCloudKitSchema)
    - .planning/research/PITFALLS.md (Pitfall 5 dedupe strategy)
    - HomeworkPlan/Repositories/SubjectRepository.swift
  </read_first>
  <action>
    Per SETT-03 and PITFALLS #5: Create CloudKitSchemaInitializer with static func initializeIfNeeded(container:) wrapped in #if DEBUG calling NSPersistentCloudKitContainer.initializeCloudKitSchema(options:) once per install (UserDefaults flag cloudKitSchemaInitialized). Invoke from HomeworkPlanApp init before ModelContainer use on DEBUG builds. Create SubjectDedupeService.mergeDuplicates(context:) — fetch all Subject, group by normalizedName, for groups count>1 keep lowest sortOrder keeper, reassign HomeworkTask relationships from duplicates to keeper, delete duplicate Subject records, save context. Unit test SubjectDedupeServiceTests: insert two Subject same normalizedName → merge leaves one. Register for NSPersistentStoreRemoteChangeNotification in HomeworkPlanApp, debounce 2s on main actor call SubjectDedupeService.mergeDuplicates. Ensure all @Model properties still have defaults/optional.
  </action>
  <acceptance_criteria>
    - CloudKitSchemaInitializer.swift exists with #if DEBUG guard
    - SubjectDedupeService.swift contains mergeDuplicates
    - SubjectDedupeServiceTests passes
    - HomeworkPlanApp.swift registers remote change observer
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:HomeworkPlanTests/SubjectDedupeServiceTests test 2>&1 | tail -3 | grep -q 'TEST SUCCEEDED'</automated>
  </verify>
  <done>CloudKit DEBUG schema init and subject dedupe logic implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Privacy manifest and README sync documentation</name>
  <files>HomeworkPlan/Resources/PrivacyInfo.xcprivacy, HomeworkPlan/README.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (PrivacyInfo.xcprivacy)
    - .planning/phases/01-daily-task-list-data-foundation/SKELETON.md
  </read_first>
  <action>
    Add PrivacyInfo.xcprivacy to target with NSPrivacyTracking false, NSPrivacyCollectedDataTypes empty array, NSPrivacyAccessedAPITypes for UserDefaults if used (CA92.1 reason). Create HomeworkPlan/README.md 中文 documenting: prerequisites Xcode 26+, iOS 17 Simulator; build command xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build; test command with -only-testing:HomeworkPlanTests; iCloud sync verification steps: (1) enable iCloud capability signed in, (2) create task on device A, (3) wait 1-2 min on device B same Apple ID, (4) confirm task appears, (5) if duplicate subjects after offline edit run dedupe on next launch. Note CloudKit container ID iCloud.app.homeworkplan.HomeworkPlan.
  </action>
  <acceptance_criteria>
    - PrivacyInfo.xcprivacy included in Xcode target Copy Bundle Resources
    - README.md contains xcodebuild build and iCloud verification numbered steps
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <done>App Store privacy manifest present; developer has sync verification runbook.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify iCloud sync and local persistence</name>
  <files>HomeworkPlan/README.md</files>
  <action>Human verifies SwiftData + CloudKit private sync with DEBUG schema init, subject dedupe on remote change, PrivacyInfo.xcprivacy, and README iCloud verification guide per SETT-03.</action>
  <what-built>SwiftData + CloudKit private sync with DEBUG schema init, subject dedupe on remote change, PrivacyInfo.xcprivacy, and README iCloud verification guide.</what-built>
  <how-to-verify>
    1. Open HomeworkPlan.xcodeproj in Xcode; confirm Signing → iCloud capability with container iCloud.app.homeworkplan.HomeworkPlan
    2. Run on Simulator signed into iCloud (or physical device)
    3. Create task「测试同步」on Simulator; if second device available, confirm it appears within 2 minutes
    4. Alternatively: verify local persistence — kill app, relaunch, task still visible (minimum bar)
    5. Run: xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' test — all tests pass
  </how-to-verify>
  <verify>
    <automated>xcodebuild -project HomeworkPlan/HomeworkPlan.xcodeproj -scheme HomeworkPlan -destination 'platform=iOS Simulator,name=iPhone 16' test 2>&1 | tail -3 | grep -q 'TEST SUCCEEDED'</automated>
    <human-check>Confirm task persists after relaunch; iCloud sync if second device available</human-check>
  </verify>
  <done>Human approves persistence minimum bar; iCloud sync verified when two devices available</done>
  <resume-signal>Type "approved" after sync or local persistence verified, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local SQLite ↔ iCloud Private DB | Homework data crosses Apple sync boundary |
| Remote change notification | Untrusted sync payloads may create duplicate entities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | CloudKit merged duplicates | mitigate | SubjectDedupeService on remote change; normalizedName business key |
| T-01-10 | Information Disclosure | iCloud homework content | accept | User-owned private CloudKit; product requirement |
| T-01-11 | Denial of Service | Schema init failure | mitigate | DEBUG-only init; log error; app continues local-only if CloudKit unavailable |
</threat_model>

<verification>
- SubjectDedupeServiceTests pass
- Full xcodebuild test suite passes
- Human confirms persistence and ideally iCloud sync
</verification>

<success_criteria>
- SETT-03 fully satisfied
- ROADMAP success criterion #5 true: data persists locally and syncs via iCloud
</success_criteria>

## Artifacts this phase produces

- `CloudKitSchemaInitializer` — DEBUG one-time schema setup
- `SubjectDedupeService.mergeDuplicates(_:)` — CloudKit duplicate subject merge
- `SubjectDedupeServiceTests` — dedupe unit tests
- `PrivacyInfo.xcprivacy` — App Store privacy manifest
- `HomeworkPlan/README.md` — build/test/sync documentation
- Remote change observer in `HomeworkPlanApp`

<output>
Create `.planning/phases/01-daily-task-list-data-foundation/01-05-SUMMARY.md` when done
</output>
