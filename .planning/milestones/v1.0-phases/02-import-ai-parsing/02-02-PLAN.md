---
phase: 02-import-ai-parsing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - HomeworkPlan/Services/Import/OCRService.swift
  - HomeworkPlan/Services/Import/ParseService.swift
  - HomeworkPlan/Services/Import/ParsePrompt.swift
  - HomeworkPlan/Services/Import/ImportService.swift
  - HomeworkPlan/App/AppDependencies.swift
  - HomeworkPlanTests/HomeworkPlanTests/ParseServiceTests.swift
autonomous: true
requirements:
  - IMPT-02
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - SETT-02
must_haves:
  truths:
    - "Vision OCR extracts text from screenshot images"
    - "ParseService calls DeepSeek with JSON mode and validates response schema"
    - "Parse retries once with stricter prompt on JSON failure"
    - "Import blocked when API key missing with clear error"
  artifacts:
    - path: HomeworkPlan/Services/Import/OCRService.swift
      provides: Vision OCR wrapper
    - path: HomeworkPlan/Services/Import/ParseService.swift
      provides: DeepSeek API actor
    - path: HomeworkPlan/Services/Import/ImportService.swift
      provides: Import pipeline orchestration
---

<objective>
OCR + DeepSeek 解析管道：Vision OCR、ParseService actor、ImportService 编排与去重。

Purpose: 将截图/粘贴文本转为 TaskCandidate 列表（不自动入库）。
Output: OCRService, ParseService, ImportService.
</objective>

<execution_context>
@/Users/bouda/work/homeworkplan/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-import-ai-parsing/02-CONTEXT.md
</context>

<artifacts this phase produces>
- OCRService.recognizeText(from: UIImage) async throws -> String
- ParseService actor parse(text:importedAt:) async throws -> [TaskCandidate]
- ParsePrompt templates (standard + strict retry)
- ImportService.processScreenshot/processPastedText pipeline
- ImportServiceError including missingAPIKey, duplicateContent
</artifacts>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| API key in network logs | medium | HTTPS only, key in Authorization header |
| Unvalidated JSON | high | Schema validation + retry |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: OCRService with Vision</name>
  <files>HomeworkPlan/Services/Import/OCRService.swift</files>
  <read_first>
    - .planning/phases/02-import-ai-parsing/02-CONTEXT.md (Vision settings)
  </read_first>
  <action>
    OCRService actor or struct: recognizeText(from image: UIImage) async throws -> String using VNRecognizeTextRequest recognitionLevel .accurate, recognitionLanguages ["zh-Hans", "en-US"]. Join recognized strings with newline. Throw OCRServiceError.noText if empty.
  </action>
  <acceptance_criteria>
    - OCRService.swift imports Vision
    - Uses VNRecognizeTextRequest with recognitionLevel accurate
  </acceptance_criteria>
  <done>OCR pipeline implemented.</done>
</task>

<task type="auto">
  <name>Task 2: ParseService DeepSeek integration</name>
  <files>HomeworkPlan/Services/Import/ParseService.swift, HomeworkPlan/Services/Import/ParsePrompt.swift, HomeworkPlanTests/HomeworkPlanTests/ParseServiceTests.swift</files>
  <read_first>
    - .planning/phases/02-import-ai-parsing/02-CONTEXT.md
    - openspec/changes/extract-mvp-scope/specs/homework-parsing/spec.md
  </read_first>
  <action>
    ParsePrompt: system prompt filtering 接龙/闲聊/通知, output JSON array schema. ParseService actor: POST https://api.deepseek.com/chat/completions model deepseek-chat (or deepseek-v3), response_format json_object, temperature 0. Inject import timestamp for relative dates. Validate decoded [TaskCandidate]. On failure retry once with strict prompt. Unit test JSON decoding with mock response payload (no live API).
  </action>
  <acceptance_criteria>
    - ParseService.swift contains actor ParseService
    - ParseServiceTests validates JSON schema decoding
    - Missing API key throws ParseServiceError.missingAPIKey
  </acceptance_criteria>
  <done>AI parsing with validation and retry.</done>
</task>

<task type="auto">
  <name>Task 3: ImportService orchestration</name>
  <files>HomeworkPlan/Services/Import/ImportService.swift, HomeworkPlan/App/AppDependencies.swift</files>
  <read_first>
    - HomeworkPlan/Repositories/ImportRepository.swift
    - HomeworkPlan/Services/Import/ContentHashService.swift
  </read_first>
  <action>
    ImportService @MainActor: dependencies on OCRService, ParseService, ImportRepository, KeychainService. processImage(UIImage) and processPastedText(String) return ImportResult with candidates, importRecord, isDuplicate flag. Check hasAPIKey first. Compute hash, skip if duplicate. Create ImportRecord, run OCR if needed, parse, store parsedJSON on record.
  </action>
  <acceptance_criteria>
    - ImportService checks keychain hasAPIKey before parse
    - Duplicate contentHash returns isDuplicate true without re-parse
  </acceptance_criteria>
  <done>End-to-end import pipeline without UI.</done>
</task>

</tasks>

<verification>
- xcodebuild build succeeds
- ParseServiceTests JSON decoding passes
</verification>
